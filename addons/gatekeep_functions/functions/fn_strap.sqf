/*
 * Author: Louetta
 * Description: Puts Comtacs Down
 *
 * Arguments:
 * None
 *
 * Return value:
 * None
 *
 * Public: No
 */
 
_curplayer = _this select 0;
	
	if(headgear _curplayer == "GateKeep_XP_Y02_2") then {
		removeHeadgear _curplayer;
		_curplayer addHeadgear "GateKeep_XP_Y02";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y15_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y15";
};_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_2_Y15_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_2_Y15";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y16_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y16";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y19_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y19";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y25_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y25";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y26_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y26";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y42_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y42";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y48_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y48";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y57_2") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y57";
};

/*
 * Author: Louetta
 * Description: Puts Comtacs Down
 *
 * Arguments:
 * None
 *
 * Return value:
 * None
 *
 * Public: No
 */
 
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y02") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y02_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y15") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y15_2";
};_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_2_Y15") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_2_Y15_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y16") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y16_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y19") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y19_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y25") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y25_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y26") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y26_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y42") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y42_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y48") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y48_2";
};
_curplayer = _this select 0;
    
    if(headgear _curplayer == "GateKeep_XP_Y57") then {
        removeHeadgear _curplayer;
        _curplayer addHeadgear "GateKeep_XP_Y57_2";
};
